{
    "name": "nuxt-app",
    "private": true,
    "scripts": {
        "build": "nuxt build",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare",
        "genProd": "npx nuxi generate"
    },
    "devDependencies": {
        "@types/node": "^18",
        "nuxt": "^3.4.3"
    },
    "dependencies": {
        "bootstrap": "^5.2.3",
        "lz-string": "^1.5.0",
        "sass": "^1.62.1"
    }
}
